version: '3.9'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: waterloo_db
      POSTGRES_USER: waterlooUser
      POSTGRES_PASSWORD: password12345
    ports:
      - "5432:5432"
volumes:
  pgdata: