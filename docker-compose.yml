version: '3.9'
services:
  mongodb:
    image: mongo:6.0
    container_name: test-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: testdb
    networks:
      - test-network
      
networks:
  test-network: